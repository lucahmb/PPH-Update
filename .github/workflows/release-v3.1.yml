name: Release PPH 3.1

on:
  push:
    paths:
      - 'src/v3.1.0/**'
      - '.github/workflows/release-v3.1.yml'

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build and validate
        run: python3 src/v3.1.0/build_release.py
      - name: Publish stable package
        run: |
          git config user.name "PPH Build Bot"
          git config user.email "actions@users.noreply.github.com"
          git add packages/v3.1.0 channels/stable.json
          git commit -m "Release PPH 3.1.0 Access Point" || exit 0
          git push
