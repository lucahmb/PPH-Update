name: Legacy PPH 3.1.0 Release (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "PPH 3.1.0 release workflow disabled; use 3.1.1 or newer."
